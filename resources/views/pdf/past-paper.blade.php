<!doctype html>
<html><head><meta charset="utf-8"><style>
body{font-family:DejaVu Sans,sans-serif;color:#17212b;font-size:12px}h1{font-size:22px;margin:0 0 6px}h2{font-size:15px;margin:22px 0 8px}.muted{color:#64748b}.cover{border-bottom:2px solid #0e7490;padding-bottom:14px;margin-bottom:20px}.q{page-break-inside:avoid;margin:0 0 20px}.q-title{font-weight:bold;margin-bottom:6px}.option{margin:4px 0 4px 18px}.lines{height:62px;border-bottom:1px solid #cbd5e1;background:repeating-linear-gradient(to bottom,transparent 0,transparent 19px,#cbd5e1 20px)}.code{float:right;color:#0e7490;font-weight:bold}
</style></head><body>
<div class="cover"><span class="code">{{ $sitting->paper_code }}</span><h1>{{ $sitting->paper->title }}</h1><div>Student: <strong>{{ $student->name }}</strong></div><div>Date: {{ $sitting->issued_at->format('j F Y') }} · Time allowed: {{ $sitting->length === 'short' ? '20 minutes' : ($sitting->length === 'standard' ? '45 minutes' : '75 minutes') }}</div><p class="muted">Show your working. Take your time. This paper is practice, not a grade.</p></div>
@foreach($questions as $question)<div class="q"><div class="q-title">{{ $question->number }}. {!! nl2br(e($question->prompt)) !!} <span class="muted">[{{ $question->marks }} mark{{ $question->marks === 1 ? '' : 's' }}]</span></div>@if($question->safe_illustration_svg)<div class="illustration">{!! $question->safe_illustration_svg !!}</div>@endif @foreach(($question->options ?? []) as $i => $option)<div class="option">{{ chr(65+$i) }}. {{ $option }}</div>@endforeach<div class="lines"></div></div>@endforeach
</body></html>
