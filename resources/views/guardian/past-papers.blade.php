<x-layouts::guardian>
<style>.pp-wrap{max-width:900px}.pp-head{display:flex;justify-content:space-between;gap:16px;align-items:end;margin-bottom:20px;flex-wrap:wrap}.pp-h1{font:600 30px Fredoka,sans-serif;margin:0;color:var(--ink)}.pp-sub{margin:4px 0 0;color:var(--ink-faint);font-weight:700}.pp-card{background:var(--paper-2);border:1px solid var(--line);border-radius:var(--radius);padding:20px;margin-bottom:16px;box-shadow:var(--shadow-sm)}.pp-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.pp-field label{display:block;font-size:11px;text-transform:uppercase;letter-spacing:.08em;font-weight:800;color:var(--ink-faint);margin-bottom:6px}.pp-field select,.pp-field input{width:100%;padding:10px;border:1px solid var(--line);border-radius:10px;background:#fff;font:600 14px Nunito}.pp-btn{display:inline-block;border:0;border-radius:11px;padding:10px 16px;background:var(--teal);color:#fff;font:800 14px Nunito;text-decoration:none;cursor:pointer}.pp-btn.gold{background:var(--amber);color:#533900}.pp-btn.ghost{background:#fff;color:var(--teal-deep);border:1px solid var(--line)}.pp-note{color:var(--ink-soft);font-size:14px;line-height:1.55}.pp-table{width:100%;border-collapse:collapse}.pp-table th,.pp-table td{text-align:left;border-bottom:1px solid var(--line);padding:11px 6px;font-size:13px}.pp-table th{font-size:11px;text-transform:uppercase;color:var(--ink-faint)}.pp-pill{display:inline-block;border-radius:999px;padding:4px 9px;font-size:11px;font-weight:800;background:var(--teal-tint);color:var(--teal-deep)}@media(max-width:620px){.pp-grid{grid-template-columns:1fr}.pp-table{font-size:12px}.pp-table th:nth-child(2),.pp-table td:nth-child(2){display:none}}</style>
<div class="pp-wrap"><div class="pp-head"><div><h1 class="pp-h1">Past papers</h1><p class="pp-sub">A real paper at the table, with a calm review afterward.</p></div><a class="pp-btn ghost" href="{{ route('guardian.dashboard') }}">← Guardian home</a></div>
@if(session('paper_created'))<div class="pp-card" role="status"><strong>Your paper is ready.</strong> Download it, give {{ $student->name }} time to sit it, then upload the pages for review.</div>@endif
<div class="pp-card"><h2 style="margin:0 0 5px;font:600 20px Fredoka">Prepare a paper for {{ $student->name }}</h2><p class="pp-note">The bank only selects approved questions from topics {{ $student->name }} has already reached. The first release uses verified answers and a guardian review step; nothing is silently graded by AI.</p><form method="POST" action="{{ route('guardian.past-papers.store', $student) }}" class="pp-grid">@csrf<div class="pp-field"><label for="subject">Subject</label><select id="subject" name="subject"><option>Math</option><option>ELA</option></select></div><div class="pp-field"><label for="length">Length</label><select id="length" name="length"><option value="short">Short · 4 questions</option><option value="standard">Standard · 10 questions</option><option value="long">Long · 20 questions</option></select></div><div><button class="pp-btn" type="submit">Create paper →</button></div></form></div>
<div class="pp-card"><h2 style="margin:0 0 12px;font:600 20px Fredoka">{{ $student->name }}'s papers</h2>@if($sittings->isEmpty())<p class="pp-note">No papers yet. Create the first one above.</p>@else<table class="pp-table"><thead><tr><th>Paper</th><th>Issued</th><th>Status</th><th></th></tr></thead><tbody>@foreach($sittings as $sitting)<tr><td><strong>{{ $sitting->paper_code }}</strong><br><span style="color:var(--ink-faint)">{{ $sitting->subject }} · {{ ucfirst($sitting->length) }}</span></td><td>{{ $sitting->issued_at->format('j M Y') }}</td><td><span class="pp-pill">{{ ucfirst(str_replace('_',' ',$sitting->status)) }}</span>@if($sitting->status==='graded')<br><strong>{{ $sitting->score }}/{{ $sitting->total_marks }}</strong>@endif</td><td><a href="{{ route('guardian.past-papers.show', [$student,$sitting]) }}" class="pp-btn ghost">Open</a></td></tr>@endforeach</tbody></table>@endif</div></div>
</x-layouts::guardian>
